[project]
name = "task-scheduling-env"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "gradio",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"